$$
"rot" bold(upright(F)) = nabla times bold(upright(F)) 
= mat(diff_x; diff_y; diff_z;) times bold(upright(F))
= mat(delim: "|",
upright(bold(i)), upright(bold(j)), upright(bold(k));
diff_x, diff_y, diff_z;
F_x, F_y, F_z;
)
$$

Отображение _Векторное поле_ -> _Векторное поле_\
Представляет собой вращение векторного поля

В праворуком определении векторного произведения:
$$
cases(
||"curl" upright(bold(F))|| > 0 <=> "вращение против часовой"\
||"curl" upright(bold(F))|| < 0 <=> "вращение по часовой"
)
$$
